// 리포트 공통 유틸 — 세 엔진(claude/gpt/gemini)이 함께 사용한다.

// 마크다운 강조 기호(**, ***) 제거.
// 리포트에 **이렇게** 남으면 기계가 쓴 티가 나므로, 프롬프트로 막고 출력에서도 한 번 더 걷어낸다.
// 표(|)·목록(-)·헤더(#)는 그대로 두고 강조 기호만 없앤다.
export function stripBoldMarkers(text) {
  if (!text || typeof text !== 'string') return text;
  return text
    .replace(/\*\*\*([\s\S]+?)\*\*\*/g, '$1')
    .replace(/\*\*([\s\S]+?)\*\*/g, '$1')
    .replace(/\*\*/g, '');   // 짝이 안 맞고 남은 것까지 정리
}

// 실행 계획 시작 시점(YYYY-MM) → 그 달부터 n개월의 라벨 배열.
// 예) '2026-09', 6 → ['2026년 9월','2026년 10월','2026년 11월','2026년 12월','2027년 1월','2027년 2월']
// 생기부는 1학년치라도 상담 시점이 2학년 2학기면 3월이 아니라 9월부터 계획을 잡아야 하므로 시작월을 받는다.
export function buildPlanMonths(startYm, n = 6) {
  const m = /^(\d{4})-(\d{1,2})$/.exec(String(startYm || '').trim());
  const now = new Date();
  let year = m ? Number(m[1]) : now.getFullYear();
  let month = m ? Number(m[2]) : now.getMonth() + 1;
  const out = [];
  for (let i = 0; i < n; i++) {
    out.push(`${year}년 ${month}월`);
    month++;
    if (month > 12) { month = 1; year++; }
  }
  return out;
}

// 내신 등급제 — 2028학년도 대입부터 5등급제, 그 이전(2027학년도까지)은 9등급제.
// 학생마다 적용 체계가 다르므로 분석·목표등급·지원전략을 그 체계에 맞춰 쓰게 한다.
export function gradeSystemGuide(gradeSystem) {
  const five = String(gradeSystem || '').includes('5');
  if (five) {
    return [
      '=== 등급 체계 (필수 준수 — 내신과 수능을 혼동하지 마라) ===',
      '- 이 학생은 2028학년도 대입 이후 대상이다.',
      '- [내신] 학교 교과 성적만 5등급제다. 내신 등급 표기·목표 등급은 반드시 1~5로 쓰고, 내신에 6~9등급을 쓰지 마라.',
      '  내신 5등급 비율: 1등급 10%, 2등급 24%(누적 34%), 3등급 32%(누적 66%), 4등급 24%(누적 90%), 5등급 10%.',
      '  전 과목 절대평가(A~E)와 상대평가 석차등급(1~5)이 병기된다는 점을 감안하라.',
      '- [수능·모의고사] 2028학년도 이후에도 9등급제를 유지한다. 수능 등급·등급 합·수능최저는 반드시 1~9로 계산하라.',
      '  국어·수학·탐구(통합사회·통합과학)는 상대평가 9등급, 영어·한국사·제2외국어/한문은 절대평가 9등급이다.',
      '  수능 등급을 5등급으로 바꾸어 쓰지 마라.',
      '- 내신 9등급 기준의 표현(예: "내신 1.5등급대")을 5등급제 내신에 그대로 적용하지 마라.',
      '- 내신 5등급제는 변별력이 낮아 교과만으로 줄세우기가 어렵다는 점을 고려해, 세특·탐구의 질과 면접·수능최저 비중을 상대적으로 더 무겁게 평가하라.',
    ].join('\n');
  }
  return [
    '=== 내신 등급 체계 (필수 준수) ===',
    '- 이 학생은 9등급제(2027학년도 대입까지) 적용 대상이다.',
    '- 모든 등급 표기·목표 등급·지원 가능선은 1~9등급 체계로 쓰라.',
    '- 9등급제 비율(1등급 4%, 2등급 11%, 3등급 23%, 4등급 40% 누적)을 기준으로 해석하라.',
    '- 5등급제(2028학년도 이후) 기준을 이 학생에게 적용하지 마라.',
  ].join('\n');
}

// 수능(모의고사) 해석 지침 — 학년도별 체제가 다르고, 대학마다 반영 지표가 달라 구분이 필요하다.
export function mockExamGuide(gradeSystem) {
  const five = String(gradeSystem || '').includes('5');
  const common = [
    '=== 수능·모의고사 해석 원칙 (필수 준수) ===',
    '- 원점수는 난이도에 따라 의미가 달라지므로 단독으로 판단하지 마라. 표준점수·백분위가 있으면 그 값을 우선 근거로 삼으라.',
    '- 대학마다 반영 지표가 다르다. 국어·수학은 표준점수를 반영하는 대학과 백분위를 반영하는 대학이 갈리고, 탐구는 백분위(또는 변환표준점수) 반영이 흔하다.',
    '  지원 전략에서는 "표준점수 반영 대학에서 유리/불리", "백분위 반영 대학에서 유리/불리"를 나누어 판단하라.',
    '- 영어·한국사·제2외국어는 절대평가라 표준점수·백분위가 없다. 등급만으로 판단하고, 대학별 영어 등급 감점/환산 방식이 당락을 가르는 점을 고려하라.',
    '- 입력된 점수가 확정(성적표)인지 예상인지 구분해 신뢰도를 달리 서술하라.',
  ];
  if (five) {
    return [
      ...common,
      '- 이 학생은 2028학년도 이후 수능(2022 개정 교육과정) 대상이다.',
      '- 수능 등급은 9등급제를 그대로 유지한다(5등급제는 내신에만 적용). 수능 등급·등급 합·수능최저는 1~9로 계산하라.',
      '- 선택과목이 폐지된 공통·통합 체제다. 국어(화법과 언어·독서와 작문·문학), 수학(대수·미적분Ⅰ·확률과 통계), 영어(영어Ⅰ·Ⅱ), 탐구는 통합사회·통합과학을 전원 응시한다.',
      '- "미적분/기하 선택", "확률과 통계 선택", "사탐·과탐 2과목 선택", 선택과목 유불리·표준점수 보정 같은 옛 체제 논리를 적용하지 마라.',
      '- 탐구는 통합사회·통합과학 두 과목 기준으로만 서술하라.',
    ].join('\n');
  }
  return [
    ...common,
    '- 이 학생은 2027학년도까지의 수능(선택과목 체제) 대상이다.',
    '- 국어는 공통+선택(화법과 작문/언어와 매체), 수학은 공통+선택(확률과 통계/미적분/기하), 탐구는 사회·과학 중 2과목 선택이다.',
    '- 선택과목에 따른 표준점수 유불리와 대학별 지정과목(예: 자연계 미적분/기하 지정, 과탐 지정) 요건을 반드시 함께 검토하라.',
  ].join('\n');
}

// 입력된 성적(내신·모의고사)을 시스템 프롬프트에 실어 모든 단계가 같은 성적을 보고 쓰게 한다.
// 예전에는 일부 단계 프롬프트에만 모의고사가 들어가, 다른 단계에서 "수능최저 확인 불가"로 처리되곤 했다.
export function scoreBlock(studentData = {}) {
  const mock = String(studentData.mockExam || '').trim();
  const gpa = String(studentData.gpa || '').trim();
  const tracks = String(studentData.targetTracks || '').trim();
  const lines = [
    '=== 학생 성적 (분석에 반드시 반영) ===',
    `- 내신: ${gpa || '미입력'}`,
    `- 모의고사: ${mock || '미입력'}`,
  ];
  if (tracks) {
    lines.push(
      '',
      '=== 희망 전형 (필수 반영) ===',
      `- 학생이 희망하는 전형: ${tracks}`,
      '- 지원 카드 6장은 이 전형 위주로 구성하고, 각 카드에 어떤 전형인지 명시하라.',
      '- 희망 전형이 학생의 성적·생기부 구조상 불리하면 그 이유를 밝히고, 대안 전형을 함께 제시하라(희망을 무시하지는 마라).',
      '- 전형 유형별 적합도 분석에서도 희망 전형을 우선 배치해 설명하라.',
    );
  }
  if (mock) {
    lines.push(
      '',
      '=== 수능최저 판정 (필수) ===',
      '- 위 모의고사 성적이 입력되어 있으므로 "수능 성적이 없어 확인할 수 없다"고 쓰지 마라.',
      '- 국어·수학·영어·탐구 등급으로 2개 합, 3개 합을 직접 계산해 표로 제시하라 (탐구는 대학 기준에 맞춰 1과목 또는 2과목 평균).',
      '- 지원 카드마다 그 대학·전형의 수능최저 기준과 비교해 충족/미충족/경계 중 하나로 판정하고 근거(등급 합)를 함께 쓰라.',
      '- 지식베이스에 그 대학의 최저 기준이 없으면 임의로 지어내지 말고, 학생의 등급 합이 어느 수준의 최저까지 충족 가능한지를 제시하고 기준은 모집요강 확인이 필요하다고 명시하라.',
      '- 확정 성적과 예상 성적을 구분해 신뢰도를 달리 서술하라.',
    );
  } else {
    lines.push('- 모의고사 미입력 상태이므로 수능최저 충족 여부는 단정하지 말고, 필요한 등급 수준을 목표로 제시하라.');
  }
  return lines.join('\n');
}

// 수시 판정 정밀 규칙 — 실제 상담 리포트가 외부 컨설턴트 검토에서 지적받은 항목을 규칙화한 것.
// (낙관 편향 사례: "최저 4번 중 3번 충족"만 보고 적정 판정 → 실제로는 영어·수학 2과목 단일 실패점,
//  모집인원 반토막 카드를 중심 카드로 유지, 모집 5명 카드에 "안정" 라벨, 통학 3시간 카드를 하한선으로 삼음)
export function strategyPrecisionGuide(gradeSystem) {
  const five = String(gradeSystem || '').includes('5');
  const lines = [
    '=== 수시 판정 정밀 규칙 (지원 전략·배치 판정에 필수 적용) ===',
    '',
    '[판정 이원화 — 전형별로 판정 방식과 신뢰도가 다르다]',
    '- 학생부교과: 대학별 환산내신과 그 대학 발표 합격컷(70%컷)을 같은 척도로 정량 비교해 판정하라.',
    '  반영교과·반영비율이 전과목 평균과 다른 대학은 환산 기준을 표 아래에 명시하라.',
    '- 학생부종합: 서류평가가 당락을 가르므로 등급만으로 안정/적정을 판정하지 마라.',
    '  기본 표기는 "지원 가능권 / 상향" 두 가지다. 예외로 ①수능최저 없음 ②서류100(면접 없음)',
    '  ③최근 컷이 학생 내신보다 0.4등급 이상 아래 — 세 조건을 모두 충족하면 "준안정(심리적 하한선)"으로',
    '  격상할 수 있다. 단 서류평가 변수가 남는다는 단서를 반드시 붙여라.',
    '- 판정 라벨은 안정 / 준안정 / 적정 / 경계 / 소신 / 상향 을 쓰고,',
    '  수능최저가 걸린 카드는 라벨 뒤에 "(최저조건부)"를 붙여 최저 미충족 시 무효임을 드러내라.',
    '',
    '[강등 규칙 — 하나라도 해당하면 판정을 최소 한 단계 보수적으로 내려라]',
    '1. 수능최저 안전마진: "몇 번 중 몇 번 충족"만 세지 말고 충족 구조를 분석하라.',
    '   충족 조합이 특정 1~2과목에만 의존하면 단일 실패점임을 명시하고,',
    '   최근 모의고사에서 미달이 한 번이라도 있으면 "안전마진 없음"으로 보고 강등하라.',
    '   최저 걸린 카드는 "충족되면 얻는 보너스" 위상으로 서술하고 6장의 중심 카드로 삼지 마라.',
    '2. 모집인원 급감: 전년 대비 모집인원이 30% 이상 줄면 합격컷이 통상 0.3~0.5등급 상승한다고 가정하라.',
    '   컷 대비 격차가 커 보여도(예: +0.9) 인원 급감과 최저 리스크가 겹치면 적정이 아니라 경계에 가깝다.',
    '3. 소수모집: 모집인원 5명 이하는 지원자 1~2명 변동에도 컷이 크게 출렁인다.',
    '   "안정" 라벨을 금지하고 최고 "적정"까지만 부여하며, 소수모집임을 근거에 명시하라.',
    '4. 통학·등록 현실성: 편도 90분(왕복 3시간)을 넘는 통학은 "합격해도 등록하지 않을 카드"일 수 있다.',
    '   이런 카드에 안정 카드(하한선) 역할을 부여하지 말고, 통학 시간을 카드 근거에 함께 적어라.',
    '',
    '[6장 균형 진단 — 지원 전략에 반드시 별도 항목으로 포함하라]',
    '- "진짜 안전판"의 정의: 수능최저 없음 + 컷 여유 충분(교과 기준 격차 +0.3등급 이상) + 등록 현실성 충족.',
    '- 6장 중 진짜 안전판이 몇 장인지 세어 명시하라. 1장 이하이면 "하한선이 얇다"고 경고하고',
    '  보강 카드(최저 없는 교과, 서류형 종합)를 구체적으로 제시하라.',
    '- 최저 걸린 카드 수를 세고, 2장을 넘으면 최저 미충족 시 동시 무효가 되는 시나리오를 서술하라.',
    '- 수능이 흔들려도 살아남는 카드(최저 없는 서류형 종합 등)의 비중을 함께 진단하고,',
    '  학생의 서류 강점(진로 일관성 등)이 있으면 이쪽 비중 확대를 검토하라.',
    '',
    '[다음 모의고사 이후 재점검 — 리포트 말미에 반드시 포함하라]',
    '- 최저 재검증: 충족이 의존하는 과목의 등급 유지 여부. 흔들리면 최저 걸린 카드를 사실상 폐기하고',
    '  종합·최저 없는 교과로 무게중심을 옮기는 대안까지 미리 적어라.',
    '- 취약 과목 반등 여부: 현재 최저 조합에서 배제된 과목이 살아나면 안전마진이 생긴다는 점을 짚어라.',
    '- 모집요강 확정본에서 모집인원·전형방법 변동 재확인.',
  ];
  if (five) {
    lines.push(
      '',
      '[5등급제 입결 해석 — 필수]',
      '- 대학이 발표한 과거 합격컷은 9등급제 기준이다. 5등급제 내신과 같은 숫자인 것처럼 직접 비교하지 마라.',
      '- 비교가 필요하면 누적 비율로 환산하라: 9등급제 누적 1등급 4% · 2등급 11% · 3등급 23% · 4등급 40% · 5등급 60%',
      '  ↔ 5등급제 누적 1등급 10% · 2등급 34% · 3등급 66% · 4등급 90%.',
      '  예: 9등급제 컷 3.0(누적 약 23%)은 5등급제로는 1등급 후반~2등급 초반에 해당한다.',
      '- 환산 근거(누적 비율)를 표나 각주로 명시하고, 전년도 컷 숫자를 5등급제 등급인 것처럼 그대로 옮겨 쓰지 마라.',
      '- 5등급제는 등급 폭이 넓어 같은 등급 안 편차가 크다. 컷 비교만으로 안정을 단정하지 말고',
      '  원점수·석차백분율 등 보조 지표와 세특의 질을 함께 근거로 삼아라.',
    );
  }
  return lines.join('\n');
}

// 위 정밀 규칙의 압축판 — 입결 콘솔 판정(judge)·리포트 분석·상담 챗·브리핑처럼
// 시스템 프롬프트가 짧아야 하는 자리에 붙인다. 라벨 체계가 4단계(안정·적정·소신·위험)로
// 고정된 화면도 있으므로 라벨 자체는 바꾸지 않고 강등·명시 규칙만 싣는다.
export function placementJudgeRules() {
  return [
    '[판정 정밀 규칙 — 필수 적용]',
    '- 수능최저가 걸린 카드: 충족 횟수만 세지 말고 어떤 과목 조합에 의존하는지 보라. 1~2과목 의존(단일 실패점)이거나',
    '  최근 모의고사 미달 이력이 있으면 판정을 한 단계 강등하고, 근거에 "최저조건부"를 명시하라.',
    '- 모집인원이 전년 대비 30% 이상 급감한 카드: 컷 0.3~0.5등급 상승 가능성을 반영해 한 단계 보수적으로 판정하라.',
    '- 모집인원 5명 이하 소수모집: 컷 변동성이 크므로 "안정" 판정을 금지하고 최고 "적정"까지만 부여하라.',
    '- 학생부종합: 등급 비교만으로 안정을 주지 마라. 서류(생기부) 근거 없이는 보수적으로 판정하라.',
    '- 6장 구성을 논할 때는 "진짜 안전판"(최저 없음+컷 여유 +0.3등급 이상+등록 현실성) 개수를 세어 말하고,',
    '  1장 이하면 하한선이 얇다고 경고하라. 최저 걸린 카드는 "충족되면 보너스" 위상으로 두라.',
  ].join('\n');
}

// 4단계(수시 지원 전략) 프롬프트 — GPT·Gemini 엔진 공용.
// Claude 엔진(claudeService.step4_strategy)은 자체 상세 포맷을 갖지만 섹션 구조는 이와 동일하게 유지한다.
export function strategyStepPrompt(studentData = {}) {
  return `[4단계: 지원 전략 수립] 학생: 내신 ${studentData.gpa} / 목표 ${studentData.targetUniv} / ${studentData.major}
수시 6장 지원 전략을 수립하라.
[출력 형식 — 절대 준수: 반드시 마크다운 표(| |)로만 출력하라. 카드 형태, 블록쿼트(>), 들여쓰기 나열 금지.]
[판정은 시스템 프롬프트의 "수시 판정 정밀 규칙"(이원화·강등 규칙·균형 진단)을 반드시 적용하라.]

판정 기준 안내 (2~3문장):
- 교과는 환산내신 vs 합격컷 정량 비교(신뢰도 높음), 종합은 서류평가가 당락을 가르므로 "지원 가능권/준안정/상향"으로만 표기함을 안내.

전형 유형 추천:
| 전형 유형 | 추천도 | 근거 |
|----------|--------|------|

학생부교과 판정:
| 대학 · 전형 | 모집단위 | 모집인원(전년→올해) | 수능최저 | 합격컷(70%) | 환산내신 | 격차 | 판정 |
|------------|---------|-----------------|---------|------------|---------|------|------|
- 판정 라벨: 안정/준안정/적정/경계/소신/상향, 최저 걸린 카드는 "(최저조건부)" 부기.
- 표 아래에 환산 기준·컷 출처 연도와 강등 사유(모집인원 급감·소수모집 등)를 각주로 명시.

수능최저 충족 구조 분석 (최저 걸린 카드가 있을 때 필수):
| 모의고사 시기 | 최선 조합 | 등급 합 | 충족 여부 |
|-------------|----------|--------|----------|
- 충족이 의존하는 과목(단일 실패점)·미달 이력·안전마진을 서술하고, 미충족 시 무효가 되는 카드를 명시.

학생부종합 지원 가능권 검토:
| 대학 · 전형 | 모집단위 | 모집인원 | 전형 방법 | 최근 경쟁률 | 최근 컷(70%) | 표기 |
|------------|---------|---------|----------|-----------|------------|------|
- 표기: 지원 가능권 / 준안정(최저 없음+서류100+컷 여유 0.4등급 이상일 때만) / 상향.

권장 지원 구성 6장 + 균형 진단:
| 성격 | 카드 | 핵심 근거 |
|------|------|----------|
- "진짜 안전판"(최저 없음+격차 +0.3등급 이상+등록 현실성) 최소 2장 확보. 표 아래에
  진짜 안전판 O장 / 최저조건부 O장 / 상향 O장을 세어 진단하고, 안전판 1장 이하이면 경고+보강 카드 제시.
- 통학 비현실 카드(편도 90분 초과)는 명시하고 하한선 역할 금지.

다음 모의고사 이후 재점검 사항:
- 최저 재검증과 미충족 시 무게중심 이동 계획, 취약 과목 반등 여부, 모집요강 확정본 재확인.`;
}

// 수시 6회 제한의 예외 — 이공계 학생에게는 카드 설계 자체가 달라지는 사실인데
// 리포트가 이를 언급하지 않아 지원 기회를 통째로 놓치는 경우가 있었다.
export function sixCardLimitGuide() {
  return [
    '=== 수시 지원 횟수 제한과 그 예외 (지원 전략에 반드시 반영) ===',
    '- 일반 대학(교육부 소관)의 수시 지원은 6회로 제한된다. 지원 카드 6장은 이 대학들로만 구성하라.',
    '- 다만 과학기술원(KAIST, GIST대학, DGIST, UNIST), POSTECH, 한국에너지공대(KENTECH)는',
    '  교육부 소관이 아니어서 이 6회 제한에 포함되지 않는다. 6장을 쓰고도 별도로 지원할 수 있다.',
    '- 따라서 이공계 지망 학생에게는 이 대학들을 "6장 밖 추가 카드"로 따로 묶어 제시하라.',
    '  6장 안에 넣어 다른 대학의 자리를 차지하게 만들지 마라.',
    '- 이 대학들은 대체로 수능최저가 없고(POSTECH 일반II 등 일부 예외) 내신을 정량 반영하지 않는 서류 정성평가다.',
    '  수능최저나 내신 정량 컷 때문에 일반 대학에서 불리한 학생도 지원 자체는 열려 있다는 점을 설명하라.',
    '  다만 지원자 풀에 과학고·영재고·전국단위 자사고가 두터우므로 합격 기대치는 냉정하게 제시하라.',
    '- 자기소개서(지원동기서 등) 제출 요건은 대학마다 다르므로 각 모집요강에서 확인하도록 안내하라.',
    '- 지역 기반 전형도 함께 점검하라. 학생의 고교 소재지에 따라 지역인재전형 자격이 생기며,',
    '  이는 경쟁 범위가 좁아 같은 내신에서도 유리하다. 자격이 있으면 반드시 카드 후보에 넣어라.',
  ].join('\n');
}

// 생기부 판독 규칙 — 리포트가 "1학년 내신 = 최종 내신", "표에서 못 봤으니 미이수"로
// 단정해 학생에게 잘못 나간 사고가 있었다. 자료 범위와 결론을 분리하도록 못박는다.
export function recordReadingGuide(studentData = {}) {
  const grade = String(studentData.grade || '').trim();
  const lines = [
    '=== 생기부 판독 규칙 (최우선 준수 — 위반 시 리포트 전체가 무효다) ===',
    '- 생기부는 학년별로 [1학년] [2학년] [3학년] 블록이 순서대로 나온다. 교과 성적표와 세특은 각 학년 블록 안에 따로 있다.',
    '- 분석을 시작하기 전에 첨부된 자료에 몇 학년치가 들어 있는지 먼저 확인하고, 그 범위를 리포트에 명시하라.',
    '- [내신 산출] 내신 등급은 반드시 확인 가능한 전 학년·전 학기 석차등급을 이수단위로 가중평균해 산출하라.',
    '  1학년 성적만 보고 산출한 값을 "학생의 내신"이나 "최종 내신"으로 쓰지 마라.',
    '  일부 학년만 반영했다면 반드시 "1학년까지 반영 기준 O.OO등급(2·3학년 미반영)"처럼 반영 범위를 등급 옆에 붙여 쓰라.',
    '- [이수 여부 판정] 특정 과목(물리학I/II, 화학, 생명과학, 미적분, 기하, 확률과통계 등)의 이수 여부는',
    '  1학년 표에 없다는 이유로 "미이수"라고 단정하지 마라. 물리·화학 등 과학 선택과목과 기하·심화수학은 보통 2·3학년에 편성된다.',
    '  자료에서 해당 학년 교과 성적표를 직접 확인하지 못했다면 "미이수"가 아니라 "확인 불가(추가 자료 필요)"로 쓰라.',
    '  "미이수"라고 쓰려면 그 학년의 교과 성적표를 실제로 확인했다는 근거(학년-학기, 과목 목록)를 함께 제시하라.',
    '- [진로선택 과목] 석차등급 없이 성취도(A/B/C)와 성취도별 분포비율로만 표기되는 과목도 이수 과목이다. 등급이 없다고 빠뜨리지 마라.',
    '- [모의고사와 이수과목 구분] 모의고사 탐구 선택과목(예: 사회·문화, 생명과학I)은 수능 응시과목일 뿐 학교 이수과목이 아니다.',
    '  모의고사 입력값을 근거로 "OO 과목을 이수했다"고 쓰지 마라. 이수과목은 오직 생기부 교과학습발달상황 표에서만 판단하라.',
    '- [입력값과 원문 충돌] 학생이 입력한 내신 등급과 생기부에서 산출한 값이 다르면, 둘을 나란히 제시하고 차이를 밝힌 뒤',
    '  생기부 원문 산출값을 기준으로 분석하라. 입력값을 말없이 그대로 쓰지 마라.',
    '- 첨부 자료에 "일부 페이지만 첨부", "앞 O자까지만 포함" 같은 표시가 있으면, 누락 구간에 대한 판단은 모두 "확인 불가"로 처리하고',
    '  리포트 서두에 어떤 자료가 빠졌는지 명시하라.',
  ];
  if (/3/.test(grade)) {
    lines.push(
      '- 이 학생은 고3이다. 1·2·3학년 성적이 모두 존재하는 것이 정상이므로, 1학년 성적만 확인된다면 자료 누락을 의심하고 그 사실을 먼저 밝혀라.',
    );
  }
  return lines.join('\n');
}

// 학생 기준 정보 블록 — 시스템 프롬프트에 붙여 모든 단계가 같은 전제로 쓰게 한다.
export function studentContextBlock(studentData = {}) {
  const months = buildPlanMonths(studentData.planStartYm, 6);
  return [
    '',
    scoreBlock(studentData),
    '',
    recordReadingGuide(studentData),
    '',
    sixCardLimitGuide(),
    '',
    gradeSystemGuide(studentData.gradeSystem),
    '',
    mockExamGuide(studentData.gradeSystem),
    '',
    strategyPrecisionGuide(studentData.gradeSystem),
    '',
    '=== 실행 계획 기준 시점 (필수 준수) ===',
    `- 상담 시점 기준으로 실행 계획은 ${months[0]}부터 시작한다.`,
    `- 월별 계획 표의 행은 정확히 다음 순서로 쓰라: ${months.join(' → ')}`,
    '- 이미 지난 달(예: 3월)로 계획을 잡지 마라. 생기부 기록이 지난 학년 것이어도 계획은 위 시점부터 세운다.',
  ].join('\n');
}
